version: '3'

services:

    # Redis service for redis things.
    redis-parcel:
        image: redis
        ports:
            - "6379:6379"
        volumes:
            - redis-parcel-vol:/data

    # MySQL service for redis things.
    mysql-parcel:
        image: redis
        environment:
            - MYSQL_ROOT_PASSWORD=root
            - MYSQL_DATABASE=angular-fastapi
            - MYSQL_USER=angular-fastapi
            - MYSQL_PASSWORD=root
        ports:
            - "3306:3306"
        volumes:
            - mysql-parcel-vol:/data

    # Postgres service for postgres things.
    postgres-parcel:
        image: postgres
        ports:
            - "5432:5432"
        volumes:
            - postgres-parcel-vol:/var/lib/postgresql


volumes:
    redis-parcel-vol:
    postgres-parcel-vol:
    mysql-parcel-vol:
